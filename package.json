{
  "name": "vector2",
  "version": "1.0.0",
  "description": "Vector2 class packed with various utilities.",
  "main": "build/Vector2.js",
  "scripts": {
    "test": "./node_modules/mocha/bin/mocha",
    "build": "tsc",
    "docs": "typedoc --out ./docs .",
    "all": "yarn && typings install && npm run build && npm run test && npm run docs && touch ./docs/.nojekyll"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "typedoc-default-themes": "0.4.0"
  },
  "typings": "build/Vector2"
}
